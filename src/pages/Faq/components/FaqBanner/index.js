const c = console.log.bind(document)

import "./style.scss"

export const FaqBanner = () => {

    const element = document.createElement("section")

    element.innerHTML = `kl`

    return element
}